{{!-- abstract this into component --}}
<nav>
    <ul>
        <li>
          {{#link-to 'index'}}Index{{/link-to}}
        </li>
        <li>
          {{#link-to 'posts'}}Posts{{/link-to}}
        </li>
    </ul>

</nav>

{{outlet}}
