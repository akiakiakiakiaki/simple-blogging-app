import { isAdmin } from '../../../helpers/is-admin';
import { module, test } from 'qunit';

module('Unit | Helper | is admin');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = isAdmin(42);
  assert.ok(result);
});
