import { dateString } from '../../../helpers/date-string';
import { module, test } from 'qunit';

module('Unit | Helper | date string');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = dateString(42);
  assert.ok(result);
});
